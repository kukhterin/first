#ifndef _HANDLER_
#define _HANDLER_

bool handler(int); //in our case we will pass to handler() the status of our execurable file exit to handle possible errors and further mapping them in statistics
	
#endif
